name: checkout code

on:
  push:

jobs:
  build my checkout code:
    runs-on: ubuntu-latest
    steps:
      - name: checkout current repository
        uses: actions/checkout@v2

      - name: run list file command
        run: ls -la

      - name: show current date
        run: date
